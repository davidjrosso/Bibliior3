services:
  - type: web
    name: biblioteca-python
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: BIBLIOTECA_DATA_DIR
        value: /opt/render/project/src/data
